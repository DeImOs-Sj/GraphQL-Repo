1) Solves the scaling issue caused by rest api 
2)Generally rest api are like pokemon.com/league/type/id
  the league endpoint scans all the league and then returns the data which causes scaling issues like 
  a)OverFetching 
  b)UnderFetching
3)GraphQL architecture image given
4)GraphQL solves the scaling issue by providing query language support using which we can easliy fetch data 
  from server by hitting single endpoint
5)Apollo server same as postman for seeing GraphQL queries
6)To get something from graphQL we use query
7)To send something to graphQL server we use Mutation 
  